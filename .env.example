# Copy to .env and fill in real values (never commit .env)
#
# Node: use 18+ (20 recommended). With nvm: `nvm use` (see .nvmrc)
#
# MongoDB — pick ONE:
# 1) Local (Docker): docker compose up -d mongo
MONGO_URI=mongodb://127.0.0.1:27017/patio-cleaning
# 2) Atlas: replace HOST with your cluster host from Atlas (e.g. cluster0.ab12cd.mongodb.net), not "cluster.mongodb.net"
# MONGO_URI=mongodb+srv://USER:PASSWORD@HOST/patio-cleaning?retryWrites=true&w=majority

PORT=5000
JWT_SECRET=change-this-to-a-long-random-string-in-production
JWT_EXPIRES_IN=7d
NODE_ENV=development

# Optional: production CORS (comma-separated origins)
# CORS_ORIGIN=https://yourdomain.com

# Optional: first admin (npm run seed:admin) — remove or rotate after use
# ADMIN_NAME=Administrator
# ADMIN_EMAIL=admin@yourdomain.com
# ADMIN_PASSWORD=use-a-strong-password-here
